# Лабораторная работа №2
## Цель работы
Изучение основных возможностей библиотеки NumPy для численных вычислений и анализа данных

## Задачи
1. Освоить создание и обработку массивов в NumPy
2. Научиться выполнять векторные и матричные операции
3. Провести статистический анализ данных
4. Визуализировать результаты с помощью matplotlib и seaborn

## Структура проекта: 
<img src="/img/struct_lab2.PNG" alt="тесты_2_лаба" width="200">

main.py - основной файл с функциями  
test.py - тесты  
plots - графики  

## Реализованные функции

### 1. Создание и обработка массивов
- `create_vector()` - создание вектора от 0 до 9 с использованием `np.arange()`
- `create_matrix()` - генерация случайной матрицы 5x5 через `np.random.rand()`
- `reshape_vector()` - изменение формы массива с помощью `reshape()`
- `transpose_matrix()` - транспонирование матриц

### 2. Векторные операции
- Сложение векторов  
- Умножение на скаляр  
- Поэлементное умножение  
- Скалярное произведение  

### 3. Матричные операции
- Умножение матриц 
- Вычисление определителя 
- Нахождение обратной матрицы 
- Решение систем линейных уравнений 

### 4. Статистический анализ
- Загрузка данных из CSV
- Вычисление статистических показателей
- Min-Max нормализация данных

### 5. Визуализация
- Гистограмма распределения оценок
- Тепловая карта корреляций
- Линейный график успеваемости


### Особенности реализации
В функциях добавлена проверка входных данных:
```python
if a.shape != b.shape:
    raise ValueError()
```
Использованы аннотации согласно PEP-484:
```python
def scalar_multiply(vec: np.ndarray, scalar: int | float) -> np.ndarray:
```  
Все графики сохранены в директорию проекта

## Тесты
<img src="/img/tests_lab2.PNG" alt="тесты_2_лаба" width="500">

## Запуск проекта
Активизация окружения:   
- source numpy_env/bin/activate   
- Windows: venv\Scripts\activate  

Запуск тестов:
- python -m pytest tests.py -v

## Итоги
- Реализована библиотека NumPy для работы с данными
- Сохранена визуализация графиков в папку plots/
- Все тесты пройдены