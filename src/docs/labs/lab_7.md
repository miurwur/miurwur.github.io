# Лабораторная работа №7  
## Задача: классификация твитов по тональности  
У нас есть выборка из твитов. Нам известна эмоциональная окраска каждого твита из выборки: положительная или отрицательная. Задача состоит в построении модели, которая по тексту твита предсказывает его эмоциональную окраску.

Классификацию по тональности используют в рекомендательных системах, чтобы понять, понравилось ли людям кафе, кино, etc.  

#[Переход на блокнот](https://colab.research.google.com/drive/1ES4AfeBxuieq79_cfs_zHbxjkHG3gYg8?usp=sharing)  


## Самостоятельная работа. Чеклист
1) ✅ Изучите материал, представленный в борде.  
2) ✅ Выполните все ячейки и получите результаты.  
3) ✅ Приведите результаты таблицы classification_report в под этим заданием для модели LogisticRegression   

- Находится в # TODO #13  
- Базовая линейная модель  
```python
              precision    recall  f1-score   support

    negative       0.73      0.78      0.75     27907
    positive       0.77      0.71      0.74     28802

    accuracy                           0.75     56709
   macro avg       0.75      0.75      0.75     56709
weighted avg       0.75      0.75      0.75     56709
```   
4) ✅ Примените 2 альтернативных использованному алгоритму для решения задачи классификации (для примера XGBClassifier и еще какой-то один) и получите результаты в таблице classification_report  

- Использовались XGBClassifier и LinearSVC  

5) ✅ Для XGBClassifier вам потребуется задать параметры  
```python
learning_rate=0.1, n_estimators=1000, max_depth=5, min_child_weight=3, gamma=0.2, subsample=0.6, colsample_bytree=1.0, objective='binary:logistic', nthread=4, scale_pos_weight=1, seed=27
```  
- Находится в конце борда

6) ✅ В разделе TF-IDF векторизация по аналогии с униграммами и пентаграммами вычислите classification_report для биграмм, триграмм опубликуйте результаты в отчете и укажите изменилась ли точность f1-score при их использовании по сравнению с униграммами и пентаграммами: 

- Вывод по разделу:  
- При переходе к n-граммам более высокого порядка наблюдается снижение качества классификации. С увеличением длины n-граммы растет разреженность матрицы признаков. Лучше всего это видно в пентаграммах. В русском языке бесконечно много уникальный словосочетаний из 5 слов, и в обучающей выборке они встречаются по 1 разу, а в тестовой не встречаются вообще. Модели не хватает данных для обучения на изолированных длинных n-граммах. Recall модели для негативных твитов показывает, что она не научилась их определять, и большинство твитов получили оценку positive  
